import React from 'react';

const Hero = () => (
    <div className="hero">
        <h1>Generate Spotify Playlists</h1>
        <p>Create custom playlists based on your favorite genres and preferences</p>
    </div>
);

export default Hero;
