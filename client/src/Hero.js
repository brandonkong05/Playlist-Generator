import React from 'react';

const Hero = () => (
    <div className="hero">
        <h1>Generate Spotify Playlists</h1>
        <p>Discover recommended tracks based on your preferences</p>
    </div>
);

export default Hero;
